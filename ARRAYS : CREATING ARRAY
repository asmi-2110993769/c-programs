int main()
{
    int n,i;
    printf("enter no. of elemnets in array:");
    scanf("%d",&n);
    int ar[n];
    for(i=0;i<n;i++)
    {
        printf("enter element at ar[%d]:",i);
        scanf("%d",&ar[i]);
    }
    printf("\n");
    for(i=0;i<n;i++)
    {
        printf("%d\n",ar[i]);
    }
}


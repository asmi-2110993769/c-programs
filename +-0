// positive,negative,zero


int main()
{
    int num;
    printf("enter num:");
    scanf("%f",&num);
    if (num>0)
        printf("positive");
    else if (num<0)
        printf("negative");
        else
            printf("Zero");
}
